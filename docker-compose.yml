version: "3.8"

services:
  web:
    build:
      context: .
    volumes:
      - .:/app/
  nginx:
    build: ./production/nginx
    volumes:
      - ./staticfiles:/app/static
    ports:
      - "8920:80"
    depends_on:
      - web